function add(a:number,b:number):number
{
    // defining functions in TypeScript 
    // fn name(params):Function_returnType
    // if not data type is passed to be returned, the function is considered void function by default
    return a+b;
}

let res = add(3,4);
console.log(res);
